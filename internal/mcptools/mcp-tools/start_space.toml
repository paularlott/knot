name = "start_space"
description = "Start a stopped space to make it available for use."
keywords = ["start", "space", "run", "launch", "activate", "boot"]

[parameters.space_name]
type = "string"
description = "Name of the space to start"
required = true
