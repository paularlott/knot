name = "stop_space"
description = "Stop a running space to free up resources."
keywords = ["stop", "space", "shutdown", "halt", "deactivate"]

[parameters.space_name]
type = "string"
description = "Name of the space to stop"
required = true
