description = "Restart all spaces in a stack."
keywords = ["restart", "stack", "reboot", "application", "reload"]
requires_approval = true
discoverable = true

[[parameters]]
name = "stack_name"
type = "string"
description = "Name of the stack to restart"
required = true

