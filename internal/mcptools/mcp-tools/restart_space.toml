description = "Restart a running space (stop and start it)."
keywords = ["restart", "space", "reboot", "reload", "cycle"]
requires_approval = true

[[parameters]]
name = "name"
type = "string"
description = "Name of the space to restart"
required = true
