description = "Start all spaces in a stack in dependency order."
keywords = ["start", "stack", "launch", "application", "dependencies"]
requires_approval = false
discoverable = true

[[parameters]]
name = "stack_name"
type = "string"
description = "Name of the stack to start"
required = true

