name = "share_space"
description = "Share a space with another user."
keywords = ["share", "space", "grant", "access", "collaborate", "permissions"]

[parameters.space_name]
type = "string"
description = "Name of the space to share"
required = true

[parameters.user_id]
type = "string"
description = "Username, email, or user ID to share the space with"
required = true
